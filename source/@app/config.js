/**
 * App configuration
 *
 * This is inner configuration that can be changed only in the build.
 */

export const availableApps = ['bus', 'air'];
export const baseUrlBus = '/bus';
export const baseUrlAir = '/air';
