/**
 * App configuration
 *
 * This is inner configuration that can be changed only in the build.
 */

export const availableApps = ['train', 'avia'];
export const baseUrlTrain = '/train';
export const baseUrlAvia = '/avia';
